{
  "name": "create-g-stack",
  "module": "index.ts",
  "bin": {
    "g-stack": "./src/index.ts"
  },
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "compile-templates": "bun run scripts/compile-templates.ts",
    "dev": "bun run compile-templates && bun run src/index.ts",
    "build": "bun run compile-templates"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clack/prompts": "^1.5.1",
    "@inquirer/prompts": "^8.5.2",
    "picocolors": "^1.1.1"
  }
}
